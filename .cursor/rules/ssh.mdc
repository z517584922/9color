---
alwaysApply: true
---

# 🔒 9Color项目SSH连接配置

## 📋 服务器信息

| 🖥️ 服务器类型 | 🌐 IP地址 | 👤 用户名 | 🔌 SSH端口 | 🔑 密码 | 📂 代码路径 |
|:---:|:---:|:---:|:---:|:---:|:---:|
| **应用服务器** | `38.180.189.204` | `root` | `28448` | `951753@sgcpj` | `/var/www/9color` |
| **数据库服务器** | `38.180.150.127` | `root` | `28447` | `159357@sgcpj` | Docker环境 |

> 💡 **注意**：SSH端口已从默认的 `9922` 更改为安全端口，所有服务都运行在Docker容器中。

---

## 🔐 SSH连接方式

### 🔑 方式一：SSH密钥认证（推荐）

```bash
# 🖥️ 应用服务器连接
ssh -i ~/.ssh/9color_rsa -p 28448 root@38.180.189.204

# 🗄️ 数据库服务器连接  
ssh -i ~/.ssh/9color_rsa -p 28447 root@38.180.150.127
```

### 🔒 方式二：密码认证（备用）

```bash
# 🖥️ 应用服务器连接
sshpass -p '951753@sgcpj' ssh -p 28448 root@38.180.189.204

# 🗄️ 数据库服务器连接
sshpass -p '159357@sgcpj' ssh -p 28447 root@38.180.150.127
```

---

## 🛡️ 安全防护措施

| 🔧 安全措施 | 📊 状态 | 📝 详细说明 |
|:---|:---:|:---|
| **端口安全** | ✅ | SSH端口更改为非标准端口（28447/28448） |
| **密钥认证** | ✅ | 配置4096位RSA密钥认证 |
| **防暴力破解** | ✅ | Fail2Ban防护（3次失败封禁1小时） |
| **防火墙保护** | ✅ | UFW防火墙规则已配置 |

---

## ⚡ 快速连接命令

### 🚀 一键连接脚本

```bash
# 添加到 ~/.bash_profile 或 ~/.zshrc
alias ssh-app='ssh -i ~/.ssh/9color_rsa -p 28448 root@38.180.189.204'
alias ssh-db='ssh -i ~/.ssh/9color_rsa -p 28447 root@38.180.150.127'

# 使用方式
ssh-app   # 连接应用服务器
ssh-db    # 连接数据库服务器
```

### 📋 常用操作

```bash
# 文件传输（应用服务器）
scp -i ~/.ssh/9color_rsa -P 28448 local_file root@38.180.189.204:/var/www/9color/

# 文件传输（数据库服务器）  
scp -i ~/.ssh/9color_rsa -P 28447 local_file root@38.180.150.127:/root/

# 端口转发（数据库访问）
ssh -i ~/.ssh/9color_rsa -p 28447 -L 3306:localhost:3306 root@38.180.150.127
```

---

## 🔍 安全状态检查

```bash
# 检查SSH服务状态
systemctl status sshd

# 检查Fail2Ban状态
fail2ban-client status

# 查看SSH登录日志
tail -f /var/log/auth.log

# 检查防火墙状态
ufw status numbered
```

---

> ⚠️ **安全提醒**：
> - 🔑 优先使用SSH密钥认证，密码认证仅作备用
> - 🚫 定期检查登录日志，发现异常及时处理  
> - 🔄 建议定期更换密码和SSH密钥
> - 📊 监控Fail2Ban封禁情况，必要时调整策略

派单逻辑
1、自动进来会怎样
创建空订单（goods_id=0），设置auto_dispatch=1，cooling_end_time=当前时间+冷却期
冷却期结束后，数据库存储过程智能匹配商品，自动扣款并立即结算完成
2. 自动进来切手动会怎样
清除冷却期(cooling_end_time=0)，设置manual_dispatch=1，auto_dispatch=0
清除已匹配的商品信息(goods_id=0, num=0, commission=0)，变为等待管理员匹配状态
3. 自动进来切手动，手动匹配商品会怎样，再切自动会怎样
手动匹配：管理员选择商品，立即扣款，设置dispatch_status=2等待结算
再切自动：检测到已扣款，设置新冷却期，等待自动结算（不重复扣款）
4. 自动进来切手动，手动不匹配商品会怎样，再切自动会怎样
不匹配商品：订单保持空状态(goods_id=0)
再切自动：重新设置冷却期，冷却结束后智能匹配商品并处理
5. 手动进来匹配商品会怎样，再切自动会怎样
匹配商品：立即扣款，dispatch_status=2等待结算
切自动：检测到已扣款，设置冷却期等待自动结算
6. 手动进来不匹配商品会怎样，再切自动会怎样
不匹配：订单保持空状态等待管理员操作
切自动：设置冷却期，系统将智能匹配商品







